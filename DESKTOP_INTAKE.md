# Папка «Выписанные пациенты»

Это дополнительный Windows-контур удобства вокруг существующего `diary-filler`.
Он **не заменяет и не изменяет** механику разбора первичного документа,
popup-диалоги, выбор создаваемых документов, заполнение шаблонов или генерацию
дневников.

## Пользовательский сценарий

1. После первого обычного запуска обновлённой программы на рабочем столе
   автоматически создаётся папка `Выписанные пациенты`.
2. В автозагрузку текущего пользователя устанавливается скрытый наблюдатель.
   Отдельная Windows-служба и права администратора не нужны.
3. Врач просто переносит первичный DOC/DOCX/DOCM или направление на госпитализацию
   в верхний уровень папки `Выписанные пациенты`.
4. Наблюдатель ждёт окончания копирования файла, проверяет, что это действительно
   первичный документ/направление, и запускает тот же `MedicalDiaryAutofill.exe`.
5. Для пациента создаётся подпапка. По умолчанию имя строится в совместимом с
   Dokkomplekt виде: `Фамилия И.О. месяц год`, например
   `Маркер М.М. май 2026`.
6. Первичный документ переносится в подпапку пациента.
7. Программа передаёт перемещённый файл в свой штатный
   `_apply_primary_document_path(...)`. С этого места работает только прежняя
   механика `diary-filler`.
8. Поскольку штатный `diary-filler` использует родительскую папку первичного
   документа как папку результата, создаваемые документы автоматически
   оказываются в подпапке пациента.

## Поддержка старого `.doc`

Старый бинарный Word `.doc` является first-class входом Windows-программы. Он
не разбирается отдельным медицинским parser'ом: программа открывает исходник
через установленный Microsoft Word в read-only режиме, один раз сохраняет
временную `.docx`-копию и передаёт именно эту копию и intake-классификатору, и
каноническому `MedicalTextParser`. Исходный `.doc` не перезаписывается.

Если Microsoft Word недоступен или COM-конвертация не сработала, штатный parser
возвращает явную ошибку с предложением сохранить документ как `.docx`; никаких
альтернативных «приблизительных» разборщиков `.doc` в проекте нет.

## Защита от ошибок

- сканируется только верхний уровень `Выписанные пациенты`; готовые подпапки
  пациентов повторно не обходятся;
- временные Word-файлы `~$...` и скрытые файлы игнорируются;
- файл не открывается, пока копирование не стабилизировалось;
- выписные/переводные/этапные эпикризы, дневники, протоколы и согласия не
  принимаются за первичный документ;
- повторно брошенный байт-в-байт тот же первичный не создаёт второго пациента;
- другой файл при совпадающем имени папки получает безопасный суффикс `(2)`,
  `(3)` и т.д.;
- дата для имени папки берётся только из уже существующего в проекте
  защищённого title-date resolver, который не принимает демографическую дату
  за дату поступления;
- если GUI уже открыт, фоновый агент не поднимает второе окно: активность GUI
  определяется техническим heartbeat без данных пациента;
- watcher использует канонический parser `diary-filler`, а не отдельный более
  строгий классификатор, поэтому документ, который штатно понимает программа,
  не должен быть молча отброшен до запуска GUI;
- `.doc`, `.docx` и `.docm` сходятся в один канонический parser boundary; для
  `.doc` временная `.docx` материализуется один раз на конкретный parse/intake
  проход и удаляется автоматически после выхода из контекста;
- Startup-VBS сохраняется в UTF-16, а резервная запись `HKCU Run` даёт второй
  пользовательский маршрут автозапуска без прав администратора;
- heartbeat агента привязан к конкретному EXE; открытый GUI проверяет его здоровье
  и автоматически перезапускает watcher, если первый скрытый запуск сорвался;
- после обновления программы старый фоновый агент видит новый handoff и уступает
  место новой версии вместо запуска вчерашнего EXE;
- технический лог агента не записывает ФИО и имена пациентских файлов;
- весь дополнительный intake работает fail-open: проблемы с Desktop/Startup не
  мешают обычному ручному запуску и прежнему созданию документов.

## Формирование имён подпапок

Движок именования поддерживает совместимые части:

- полное ФИО;
- фамилия + инициалы;
- фамилия + имя;
- дата поступления;
- дата выписки;
- обе даты;
- месяц поступления;
- месяц выписки.

Безопасный intake-default — `surname_initials + admission_month`, потому что эти
данные известны уже в момент переноса первичного документа. Части, зависящие от
даты выписки, поддерживаются движком, но intake не выдумывает отсутствующую дату
и не вмешивается ради неё в генерацию документов.

## Архитектурная граница

Чтобы не нарушать действующий лимит архитектуры `125/125` runtime Python-файлов,
новый контур не размазан по дополнительным микромодулям. Он встроен в уже
существующий внешний `startup.py`; `main.py` только включает агентный режим и
передаёт GUI в этот startup-контур.

Критическая точка интеграции одна: готовый путь передаётся в существующий
`app._apply_primary_document_path(...)`. Модули генерации медицинских документов,
дневников, шаблоны и правила рендеринга desktop-intake не изменяет.

Для диагностики автоматический intake можно временно отключить переменной:

```bat
set MEDICAL_AUTOFILL_DISABLE_DESKTOP_INTAKE=1
```

Headless-контракт находится вне runtime-корня и не расходует архитектурный
лимит:

```bat
python tests/desktop_intake_contract_check.py
```

Контракт отдельно доказывает `.docx`, `.docm` и legacy `.doc`; для `.doc` в
headless-тесте подменяется только внешняя Microsoft Word COM-конвертация, а весь
последующий путь остаётся реальным и должен вызвать shared converter ровно один
раз.

## Обязательный Windows E2E

CI после сборки EXE воспроизводит пользовательский путь целиком: запускает GUI,
убеждается, что watcher поднялся, закрывает GUI, кладёт настоящий первичный DOCX
в `Выписанные пациенты` и требует, чтобы watcher сам запустил EXE с
`--intake-primary`, а документ оказался в подпапке пациента. Этот E2E является
release-блокером, а не диагностикой «для информации».

Последний post-merge baseline `c0d0285c6c40d308605f003d5481dfa7ff4f99d8`
прошёл Windows run `35244297698` полностью: 34/34, включая desktop intake
contract, lifecycle regression, real Tk wiring, packaged EXE startup/TkDND,
auto-launch E2E, installer и install/uninstall smoke.
