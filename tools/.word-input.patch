diff --git a/build_exe_windows.bat b/build_exe_windows.bat
index 282dc36..3a9d843 100644
--- a/build_exe_windows.bat
+++ b/build_exe_windows.bat
@@ -87,6 +87,9 @@ python -m PyInstaller ^
   --collect-all tkinterdnd2 ^
   --hidden-import win32api ^
   --hidden-import win32print ^
+  --hidden-import pythoncom ^
+  --hidden-import win32com ^
+  --hidden-import win32com.client ^
   main.py
 
 if exist dist\MedicalDiaryAutofill.exe (
diff --git a/diary_text_parser.py b/diary_text_parser.py
index 071fd4a..e885c97 100644
--- a/diary_text_parser.py
+++ b/diary_text_parser.py
@@ -5,13 +5,14 @@
 
 from __future__ import annotations
 
-import os
 import re
 from pathlib import Path
 from tempfile import TemporaryDirectory
 
 from docx import Document
 
+from medical_docx_blocks import convert_legacy_doc_to_docx
+
 from diary_constants import (DATE_PREFIX_RE, EXAMINEE_ANY_RE, EXAMINEE_STANDARD_STATE_RE, EXAMINEE_START_RE, MIN_STATUS_LEN, SIGNATURE_MARKERS, STATUS_DATE_PREFIX_RE, STATUS_LABEL_PREFIX_RE, STATUS_NUMBER_BEFORE_DATE_RE, STATUS_NUMBER_PREFIX_RE, STATUS_STANDALONE_DAY_PREFIX_RE, STRUCTURAL_DIARY_PREFIXES, WHITESPACE_RE)
 
 def normalize_text(text: str) -> str:
@@ -88,59 +89,8 @@ def looks_like_status(text: str) -> bool:
 
 
 def _convert_legacy_doc_to_docx(source: Path, target: Path) -> None:
-    """Convert a legacy binary .doc using Microsoft Word on Windows.
-
-    python-docx cannot read the old OLE .doc format. The desktop application
-    already depends on pywin32 on Windows, so we convert into a temporary DOCX
-    and then run the exact same tested parser used for native DOCX files.
-    """
-    if os.name != "nt":
-        raise ValueError(
-            "Старый формат .doc поддерживается в установленной Windows-программе через Microsoft Word. "
-            "На этой системе сохраните файл как .docx."
-        )
-    try:
-        import pythoncom
-        import win32com.client
-    except Exception as exc:
-        raise ValueError(
-            "Не удалось подключить поддержку .doc через Microsoft Word. "
-            "Сохраните файл как .docx или переустановите программу."
-        ) from exc
-
-    word = None
-    opened = None
-    pythoncom.CoInitialize()
-    try:
-        word = win32com.client.DispatchEx("Word.Application")
-        word.Visible = False
-        word.DisplayAlerts = 0
-        opened = word.Documents.Open(
-            str(source.resolve()),
-            ReadOnly=True,
-            AddToRecentFiles=False,
-            ConfirmConversions=False,
-        )
-        # wdFormatXMLDocument == 12. SaveAs2 preserves the document while making
-        # it readable by python-docx; the user's original .doc is never modified.
-        opened.SaveAs2(str(target.resolve()), FileFormat=12, AddToRecentFiles=False)
-    except Exception as exc:
-        raise ValueError(
-            f"Не удалось прочитать старый Word-файл .doc: {source.name}. "
-            "Для .doc требуется установленный Microsoft Word; можно также сохранить файл как .docx."
-        ) from exc
-    finally:
-        if opened is not None:
-            try:
-                opened.Close(False)
-            except Exception:
-                pass
-        if word is not None:
-            try:
-                word.Quit()
-            except Exception:
-                pass
-        pythoncom.CoUninitialize()
+    """Compatibility wrapper around the shared Word input converter."""
+    convert_legacy_doc_to_docx(source, target)
 
 
 def _open_status_document(path: Path):
diff --git a/dnd_mixin.py b/dnd_mixin.py
index 06c40e6..13637a9 100644
--- a/dnd_mixin.py
+++ b/dnd_mixin.py
@@ -184,7 +184,7 @@ class DragDropMixin:
             if "эпи" in stem_low or txt_low.strip().startswith("эпи") or "эпи:" in txt_low or "эпидемиологическ" in txt_low:
                 return "epi"
             return "unknown"
-        if suffix not in {".docx", ".docm"}:
+        if suffix not in {".doc", ".docx", ".docm"}:
             return "unknown"
 
         text = ""
diff --git a/files_mixin.py b/files_mixin.py
index 29ed561..98bef25 100644
--- a/files_mixin.py
+++ b/files_mixin.py
@@ -277,7 +277,7 @@ class FilesMixin:
         path = filedialog.askopenfilename(
             title="Выберите первичный документ",
             initialdir=self._dialog_initial_dir(DIR_PRIMARY_DOCUMENTS),
-            filetypes=[("Word DOCX", "*.docx")],
+            filetypes=[("Word документы (.doc/.docx/.docm)", ("*.doc", "*.docx", "*.docm"))],
         )
         if not path:
             return
@@ -287,7 +287,7 @@ class FilesMixin:
         path = filedialog.askopenfilename(
             title="Выберите файл ЭПИ",
             initialdir=self._dialog_initial_dir(DIR_EPI),
-            filetypes=[("Word DOCX/DOCM", "*.docx *.docm"), ("Text", "*.txt"), ("All files", "*.*")],
+            filetypes=[("Word документы (.doc/.docx/.docm)", ("*.doc", "*.docx", "*.docm")), ("Text", "*.txt"), ("All files", "*.*")],
         )
         if path:
             self.epi_path_var.set(path)
diff --git a/medical_docx_blocks.py b/medical_docx_blocks.py
index cc558de..8d2c867 100644
--- a/medical_docx_blocks.py
+++ b/medical_docx_blocks.py
@@ -5,12 +5,15 @@
 
 from __future__ import annotations
 
+import os
 import re
 import threading
 import zipfile
+from contextlib import contextmanager
 import xml.etree.ElementTree as ET
 from datetime import datetime
 from pathlib import Path
+from tempfile import TemporaryDirectory
 from typing import Iterable, List, Optional
 
 from docx import Document
@@ -27,6 +30,76 @@ _DOCX_TEXT_CACHE_LOCK = threading.Lock()
 _DOCX_TEXT_CACHE: dict[str, tuple[int, int, str]] = {}
 
 
+_SUPPORTED_WORD_SUFFIXES = {".doc", ".docx", ".docm"}
+
+
+def convert_legacy_doc_to_docx(source: Path, target: Path) -> None:
+    """Convert old binary Word .doc into a temporary DOCX without touching source."""
+    if os.name != "nt":
+        raise ValueError(
+            "Старый формат .doc поддерживается в установленной Windows-программе через Microsoft Word. "
+            "На этой системе сохраните файл как .docx."
+        )
+    try:
+        import pythoncom
+        import win32com.client
+    except Exception as exc:
+        raise ValueError(
+            "Не удалось подключить поддержку .doc через Microsoft Word. "
+            "Сохраните файл как .docx или переустановите программу."
+        ) from exc
+
+    word = None
+    opened = None
+    pythoncom.CoInitialize()
+    try:
+        word = win32com.client.DispatchEx("Word.Application")
+        word.Visible = False
+        word.DisplayAlerts = 0
+        opened = word.Documents.Open(
+            str(source.resolve()),
+            ReadOnly=True,
+            AddToRecentFiles=False,
+            ConfirmConversions=False,
+        )
+        # wdFormatXMLDocument == 12. The original .doc remains unchanged.
+        opened.SaveAs2(str(target.resolve()), FileFormat=12, AddToRecentFiles=False)
+    except Exception as exc:
+        raise ValueError(
+            f"Не удалось прочитать старый Word-файл .doc: {source.name}. "
+            "Для .doc требуется установленный Microsoft Word; можно также сохранить файл как .docx."
+        ) from exc
+    finally:
+        if opened is not None:
+            try:
+                opened.Close(False)
+            except Exception:
+                pass
+        if word is not None:
+            try:
+                word.Quit()
+            except Exception:
+                pass
+        pythoncom.CoUninitialize()
+
+
+@contextmanager
+def materialize_word_source_as_docx(path: str | Path):
+    """Yield a python-docx-readable path for .docx/.docm/.doc inputs."""
+    source = Path(path).expanduser()
+    suffix = source.suffix.lower()
+    if suffix not in _SUPPORTED_WORD_SUFFIXES:
+        allowed = ", ".join(sorted(_SUPPORTED_WORD_SUFFIXES))
+        raise ValueError(f"Неверный формат Word-файла: {suffix or 'без расширения'}. Разрешено: {allowed}.")
+    if suffix != ".doc":
+        yield source
+        return
+    with TemporaryDirectory(prefix="medical-autofill-legacy-doc-") as tmp_dir:
+        converted = Path(tmp_dir) / (source.stem + ".docx")
+        convert_legacy_doc_to_docx(source, converted)
+        yield converted
+
+
 def _docx_text_cache_key(path: Path) -> str:
     try:
         return str(path.resolve())
@@ -107,7 +180,6 @@ def extract_docx_text(path: str | Path) -> str:
     if cached_text is not None:
         return cached_text
 
-    doc = Document(str(candidate))
     lines: List[str] = []
 
     def walk(parent):
@@ -126,7 +198,9 @@ def extract_docx_text(path: str | Path) -> str:
                         seen_cells.add(tc_id)
                         walk(cell)
 
-    walk(doc)
+    with materialize_word_source_as_docx(candidate) as readable_path:
+        doc = Document(str(readable_path))
+        walk(doc)
     text = normalize_text("\n".join(lines))
 
     # Cache only a stable read. If Word/Explorer modified or replaced the file
diff --git a/medical_docx_title_finder.py b/medical_docx_title_finder.py
index 04ec40f..92a0d2e 100644
--- a/medical_docx_title_finder.py
+++ b/medical_docx_title_finder.py
@@ -9,7 +9,7 @@ from docx.text.paragraph import Paragraph
 
 from medical_constants import DATE_FMT
 from medical_text_utils import normalize_match, normalize_text
-from medical_docx_blocks import iter_block_items
+from medical_docx_blocks import iter_block_items, materialize_word_source_as_docx
 from medical_docx_date_patterns import _TITLE_DATE_RE, _first_valid_full_date, _normalize_full_date_match
 from medical_docx_title_context import (
     _date_match_has_birth_context,
@@ -92,6 +92,14 @@ def _extract_admission_date_from_title_docx_uncached(path: str | Path) -> str:
     if filename_date:
         return filename_date
 
+    try:
+        source = Path(path).expanduser()
+        if source.suffix.lower() == ".doc":
+            with materialize_word_source_as_docx(source) as converted:
+                return _extract_admission_date_from_title_docx_uncached(converted)
+    except Exception:
+        return ""
+
     structured_entries: list[str] = []
     row_entries: list[list[str]] = []
 
diff --git a/medical_service.py b/medical_service.py
index 6ba5c8c..34ae376 100644
--- a/medical_service.py
+++ b/medical_service.py
@@ -37,9 +37,9 @@ from medical_renderer import MedicalDocumentRenderer
 
 
 _TEXT_ENCODINGS = ("utf-8-sig", "utf-8", "cp1251")
-_PRIMARY_SUFFIXES = {".docx", ".docm"}
+_PRIMARY_SUFFIXES = {".doc", ".docx", ".docm"}
 _EPI_TEXT_SUFFIXES = {".txt"}
-_EPI_DOCX_SUFFIXES = {".docx", ".docm"}
+_EPI_DOCX_SUFFIXES = {".doc", ".docx", ".docm"}
 
 
 class MedicalDocumentService:
diff --git a/smoke_combined_part01_setup_contracts.py b/smoke_combined_part01_setup_contracts.py
index 8560e99..2a33ff6 100644
--- a/smoke_combined_part01_setup_contracts.py
+++ b/smoke_combined_part01_setup_contracts.py
@@ -139,8 +139,11 @@ assert "Ничего не запоминаем между разными popup-
 assert "self._last_committee_date,\n            self.vk_date_var.get().strip()" not in main_source
 assert "self.vk_date_var.get().strip(),\n            self.sick_leave_vk_commission_date_var.get().strip()" not in main_source
 assert 'single_line=self._compact_ui' in main_source
-assert 'suffix not in {".docx", ".docm"}' in main_source
+assert 'suffix not in {".doc", ".docx", ".docm"}' in main_source
 assert 'txt_low.strip().startswith("эпи")' in main_source
+build_script_source = (ROOT / "build_exe_windows.bat").read_text(encoding="utf-8")
+for hidden_import in ("pythoncom", "win32com", "win32com.client"):
+    assert f"--hidden-import {hidden_import}" in build_script_source, hidden_import
 
 release_zip_source = (ROOT / "make_release_zip.py").read_text(encoding="utf-8")
 for snippet in ['".spec"', '".DS_Store"', '"Thumbs.db"', '".vscode"', '".idea"']:
diff --git a/smoke_combined_part06_diary_columns_settings.py b/smoke_combined_part06_diary_columns_settings.py
index 5fdaa0d..759bdc7 100644
--- a/smoke_combined_part06_diary_columns_settings.py
+++ b/smoke_combined_part06_diary_columns_settings.py
@@ -362,9 +362,9 @@ non_docx_primary = OUT / "primary_wrong_type.txt"
 non_docx_primary.write_text("Первичный осмотр", encoding="utf-8")
 try:
     service.parse_primary_document(non_docx_primary)
-    raise AssertionError("primary parser must reject non-DOCX files before python-docx")
+    raise AssertionError("primary parser must reject non-Word files before parsing")
 except ValueError as exc:
-    assert "первичный документ" in str(exc) and ".docx" in str(exc), str(exc)
+    assert "первичный документ" in str(exc) and ".doc" in str(exc) and ".docx" in str(exc), str(exc)
 
 bad_epi = OUT / "epi_wrong_type.rtf"
 bad_epi.write_text("ЭПИ: текст", encoding="utf-8")
diff --git a/startup.py b/startup.py
index 1bf5b22..44a6599 100644
--- a/startup.py
+++ b/startup.py
@@ -65,7 +65,7 @@ def _create_root(*, require_dnd: bool = False):
 DESKTOP_INTAKE_FOLDER_NAME = "Выписанные пациенты"
 DESKTOP_INTAKE_AGENT_ARGUMENT = "--intake-agent"
 DESKTOP_INTAKE_PRIMARY_ARGUMENT = "--intake-primary"
-_DESKTOP_INTAKE_SUPPORTED_EXTENSIONS = {".docx", ".docm"}
+_DESKTOP_INTAKE_SUPPORTED_EXTENSIONS = {".doc", ".docx", ".docm"}
 _DESKTOP_INTAKE_QUIET_SECONDS = 1.5
 _DESKTOP_INTAKE_GUI_POLL_MS = 1600
 _DESKTOP_INTAKE_HEARTBEAT_MS = 1800
diff --git a/tests/desktop_intake_contract_check.py b/tests/desktop_intake_contract_check.py
index 863ea3d..cc2f429 100644
--- a/tests/desktop_intake_contract_check.py
+++ b/tests/desktop_intake_contract_check.py
@@ -1,6 +1,7 @@
 """Headless contract checks for the optional desktop patient-folder workflow."""
 from __future__ import annotations
 
+import shutil
 import sys
 import tempfile
 import time
@@ -14,6 +15,8 @@ if str(ROOT) not in sys.path:
     sys.path.insert(0, str(ROOT))
 
 import startup
+import medical_docx_blocks
+from medical_service import MedicalDocumentService
 
 
 def _assert_naming_contract() -> None:
@@ -59,6 +62,7 @@ def _assert_primary_detection_contract() -> None:
     assert startup.desktop_intake_primary_score(primary) >= 5
     assert startup.desktop_intake_primary_score(referral) >= 5
     assert startup.desktop_intake_primary_score(discharge) < 0
+    assert startup.desktop_intake_is_candidate_word_file("Первичный.doc")
     assert startup.desktop_intake_is_candidate_word_file("Первичный.docx")
     assert startup.desktop_intake_is_candidate_word_file("Первичный.docm")
     assert not startup.desktop_intake_is_candidate_word_file("~$Первичный.docx")
@@ -81,6 +85,31 @@ def _assert_canonical_primary_parser_contract() -> None:
         doc.save(canonical)
         assert startup.desktop_intake_is_primary_document(canonical), "canonical parser primary was rejected"
 
+        # DOCM uses the same OOXML reader path and must remain first-class input.
+        macro = root / "patient.docm"
+        shutil.copyfile(canonical, macro)
+        macro_data = MedicalDocumentService().parse_primary_document(macro)
+        assert macro_data.fio == "Иванов Иван Иванович", macro_data.fio
+        assert startup.desktop_intake_is_primary_document(macro), "DOCM primary was rejected"
+
+        # Real binary DOC conversion requires Microsoft Word and is therefore
+        # exercised on Windows through the same converter. Here we replace only
+        # the external COM conversion with a deterministic copy so the contract
+        # proves every caller routes .doc through the shared conversion boundary.
+        legacy = root / "patient.doc"
+        legacy.write_bytes(b"legacy-doc-placeholder")
+        original_converter = medical_docx_blocks.convert_legacy_doc_to_docx
+        try:
+            medical_docx_blocks.convert_legacy_doc_to_docx = (
+                lambda _source, target: shutil.copyfile(canonical, target)
+            )
+            legacy_data = MedicalDocumentService().parse_primary_document(legacy)
+            assert legacy_data.fio == "Иванов Иван Иванович", legacy_data.fio
+            assert legacy_data.admission_date == "12.05.2026", legacy_data.admission_date
+            assert startup.desktop_intake_is_primary_document(legacy), "DOC primary was rejected"
+        finally:
+            medical_docx_blocks.convert_legacy_doc_to_docx = original_converter
+
         discharge = root / "discharge.docx"
         doc = Document()
         doc.add_paragraph("Выписной эпикриз")
diff --git a/tools/document_mechanics_change_approval.json b/tools/document_mechanics_change_approval.json
index e44f83f..6c074cb 100644
--- a/tools/document_mechanics_change_approval.json
+++ b/tools/document_mechanics_change_approval.json
@@ -1,9 +1,12 @@
 {
   "schema_version": 1,
-  "base_sha": "9bd3278a2b51694d7eae5ecc98f4011b342d0859",
-  "reason": "Explicit document-mechanics task requested by the user: restore the proven Dokkomplekt sick-leave dynamic epicrisis layer while preserving the established regular diary generator and the canonical staff-profile signature wiring required by production audit. The change remains additive: sick leave off leaves existing diary generation untouched; sick leave on adds 10-day dynamic epicrises with non-working-day shift and a hard discharge boundary. To preserve the production runtime-file budget, the dynamic-epicrisis helpers are kept inside the existing diary_service.py rather than a new runtime module. Approval is bound to the exact blobs below and does not authorize any other protected path.",
+  "base_sha": "c8c9f2ec94fc92403b3a0e7aba125dc7b93aff36",
+  "reason": "Explicit Word-input compatibility task requested by the user: make primary/input Word handling accept .doc, .docx and .docm consistently. Legacy binary .doc is converted read-only through Microsoft Word into a temporary DOCX and then passed through the same established parser as native OOXML. The original .doc is never modified. This change is limited to Word input/read boundaries, file selection and intake detection; document rendering, diary cadence, generated document text and diary_batch.py remain unchanged. Approval is bound to the exact protected blobs below.",
   "approved_blobs": {
-    "actions_diary_flow.py": "bebc881c8fa6319aa0cb5797b7f546a1ed7a8bf5",
-    "diary_service.py": "9a43e7dde10ceae6557ef3ef600e2af3b1accb29"
+    "diary_text_parser.py": "e885c971954921147a14939f67fb25c4ada1d196",
+    "files_mixin.py": "98bef25c802fe7fc248a5b6c6c8a9dacb001294e",
+    "medical_docx_blocks.py": "8d2c8676969675ba2a4715cfb3348ae6d37a8de2",
+    "medical_docx_title_finder.py": "92a0d2ee628d9c80cd4c5865deac2f0af0873ad5",
+    "medical_service.py": "34ae376c2cc7e16801ef08105a90137c9fb44057"
   }
 }
diff --git a/tools/regression_lock_change_approval.json b/tools/regression_lock_change_approval.json
new file mode 100644
index 0000000..added22
--- /dev/null
+++ b/tools/regression_lock_change_approval.json
@@ -0,0 +1,31 @@
+{
+  "schema_version": 1,
+  "baseline_ref": "66b053fe883487dea55376181803fa41c10dbe68",
+  "reason": "User-requested Word input compatibility fix: .doc, .docx and .docm are accepted consistently by primary document service, manual selection, drag-and-drop and desktop intake. Legacy .doc is read-only converted through the shared Word COM boundary into a temporary DOCX and then uses the established parser. No document renderer, diary schedule or generated document content is changed. Exact candidate drift is approved only for the blobs listed here and requires the complete repository evidence contour before baseline advancement.",
+  "required_evidence": [
+    "ci_gate_lock",
+    "desktop_intake_contract",
+    "document_mechanics_guard",
+    "full_patient_replay",
+    "gender_generation_matrix",
+    "golden_docx",
+    "installer_uninstall_smoke",
+    "intake_lifecycle_regression",
+    "packaged_desktop_intake_e2e",
+    "staff_profile_regression"
+  ],
+  "approved_blobs": {
+    "build_exe_windows.bat": "3a9d84349bc13e7babf486b7029b66a9a906efba",
+    "diary_text_parser.py": "e885c971954921147a14939f67fb25c4ada1d196",
+    "dnd_mixin.py": "13637a986f4c1cc0a7355bd327ac7c4099627021",
+    "files_mixin.py": "98bef25c802fe7fc248a5b6c6c8a9dacb001294e",
+    "medical_docx_blocks.py": "8d2c8676969675ba2a4715cfb3348ae6d37a8de2",
+    "medical_docx_title_finder.py": "92a0d2ee628d9c80cd4c5865deac2f0af0873ad5",
+    "medical_service.py": "34ae376c2cc7e16801ef08105a90137c9fb44057",
+    "smoke_combined_part01_setup_contracts.py": "2a33ff67709b06812e911db535ba3eda4e2ffcad",
+    "smoke_combined_part06_diary_columns_settings.py": "759bdc75723d1d90057065d4a5634c5f39d6d0d0",
+    "startup.py": "44a65992b66495a0dfb5dd414c8e1d2e57683b8b",
+    "tests/desktop_intake_contract_check.py": "cc2f429a6b1c8403d774e846b6feecf43b4f09df",
+    "tools/document_mechanics_change_approval.json": "6c074cbb68cc6ee46c2f78456c3a8159ca101daf"
+  }
+}
