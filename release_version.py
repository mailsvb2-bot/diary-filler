from __future__ import annotations

# Release/build identity intentionally lives outside protected document mechanics.
# Keep this synchronized with pyproject.toml, version_info.txt and the installer.
APP_VERSION = "v1.4.5-intake-lifecycle"
APP_VERSION_NUMBER = "1.4.5"
