name: Apply clean Word parser follow-up

on:
  push:
    branches:
      - fix/word-primary-canonical-parser-followup
    paths:
      - tools/.word-parser-followup-clean.patch
      - .github/workflows/apply-word-parser-followup.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: fix/word-primary-canonical-parser-followup
          fetch-depth: 0
      - name: Apply exact follow-up patch
        shell: bash
        run: |
          set -euo pipefail
          git apply --check tools/.word-parser-followup-clean.patch
          git apply tools/.word-parser-followup-clean.patch
          rm tools/.word-parser-followup-clean.patch
          rm .github/workflows/apply-word-parser-followup.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: route legacy doc through one canonical parser file'
          git push origin HEAD:fix/word-primary-canonical-parser-followup
