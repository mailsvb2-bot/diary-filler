name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build-windows-exe:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install build dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements_build.txt

      - name: Build one-file Windows app
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $appPath = "src\zapolnitel_dnevnikov_app.py"
          if (-not (Test-Path $appPath)) { throw "Application file not found: $appPath" }

          pyinstaller `
            --noconfirm `
            --clean `
            --onefile `
            --windowed `
            --name "ZapolnitelDnevnikov" `
            --collect-all docx `
            --collect-all lxml `
            $appPath

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: ZapolnitelDnevnikov-Windows-EXE
          path: dist/ZapolnitelDnevnikov.exe
          retention-days: 30
